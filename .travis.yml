language: php

php:
  - 5.6
  - 7

sudo: false

before_install:
  - travis_retry composer self-update

install:
  - travis_retry composer install --dev --no-interaction --prefer-dist

script:
  - ./vendor/bin/phpunit
